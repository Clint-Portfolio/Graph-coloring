# Add the file-structure to paths
import os, sys
directory = os.path.dirname(os.path.realpath(__file__))
sys.path.append(os.path.join(directory, "Algoritmes"))
sys.path.append(os.path.join(directory, "Algoritmes", ""))

#imports
import helpers

#code
def main():
    if x:
        run random
    else:
        run greedy
